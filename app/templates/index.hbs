<div class="container">

  {{#if model}}
    {{#each model as |category|}}
      <div class="row well">
        <div class="col-md-12">
          {{#link-to 'category' category.id }}{{category.name}}{{/link-to}}
        </div>
      </div>
    {{/each}}

  {{else}}<!-- Add a category -->
  {{/if}}
  {{new-cat model=model saveCat="saveCat"}}
</div>
